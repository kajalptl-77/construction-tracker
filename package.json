{
  "name": "construction-tracker",
  "version": "1.0.0",
  "description": "Master Construction Builder Tracker - Full Stack",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend start\"",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend start",
    "install:all": "npm install & npm --prefix backend install & npm --prefix frontend install"
  },
  "keywords": ["construction", "tracker", "builder"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
